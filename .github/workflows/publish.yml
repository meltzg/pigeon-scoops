name: Publish Docker Image

# Tag-driven (usb-ci FR-10.1): fires when a v#.#.# tag lands on GitHub --
# after migration, via the Forgejo push mirror, whose PAT-authenticated
# pushes fire workflows (verified: usb-ci #27, pigeon-scoops-backend
# v1.0.0). CI runs on the forge; this repo on GitHub is a read-only mirror
# and publication is its only remaining job (usb-ci FR-10.4). Docker Hub
# updates when a release is tagged, not on every merge.

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Version from tag
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: vadercows/pigeon-frontend:latest,vadercows/pigeon-frontend:${{ env.VERSION }}
