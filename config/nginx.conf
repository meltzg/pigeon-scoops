server {
    listen 80 default_server;
    server_name _;

    location / {
        root /usr/share/nginx/html;
        
        # Try to serve the file, then the directory, otherwise serve index.html
        # This allows the SPA router to handle all routes
        try_files $uri $uri/ /index.html;
    }
}
